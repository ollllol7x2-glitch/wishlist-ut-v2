# v2 모바일 사용성 테스트 프로토타입 검토

검토 기준:

- `output/prototypes/wishlist-a1/facilitator.html`
- `output/prototypes/wishlist-a1/index.html`
- `output/prototypes/wishlist-a1/app.js`
- `13_사용성테스트/찜_기존-개선안_사용성테스트_설계_v2_0901.md`

## 결론

이번 테스트는 모바일로 진행하는 것이 맞다. 실제 지그재그 앱과 개선안을 같은 기기·유사한 화면 크기·같은 손 조작 조건에서 비교할 수 있기 때문이다.

다만 기존 PC형 진행자 화면을 단순히 좁게 줄이는 방식은 적절하지 않다. 모바일에서는 다음 구조로 바꾸는 것이 좋다.

1. 모바일 세션 홈에서 참여자와 과업을 선택한다.
2. 참여자가 과업 문구와 종료 시점을 읽는다.
3. 참여자가 `과업 시작`을 직접 누른다.
4. 기존 서비스 조건은 실제 지그재그 앱으로 이동해 수행한다.
5. 개선안 조건은 모바일 웹 프로토타입에서 수행한다.
6. 과업 종료 후 같은 화면에서 SEQ와 후속 질문에 응답한다.
7. 모든 기록은 브라우저 저장소에 누적하고 세션 마지막에 CSV 한 개로 저장한다.

## 기존 테스트에서 재사용할 기능

| 기능 | 판단 | 모바일 v2 반영 방식 |
| --- | --- | --- |
| 참여자 ID 선택 | 재사용 | P1~P5 선택 후 모든 과업에 자동 연결 |
| 참여자의 직접 시작 | 재사용 | 시작 전에는 시간·탭 기록 금지 |
| 시작·종료 시각 | 재사용 | `Date.now()` 원시 시각과 소요 밀리초 저장 |
| 클릭 순서 | 재사용 | 개선안에서 `pointerup` 기준으로 자동 기록 |
| 클릭 대상 문구·상품 ID | 재사용 | 화면 ID, 동작, 대상 문구, 상품 ID를 함께 저장 |
| 정규화 좌표 | 재사용 | CSS 좌표와 화면 대비 X·Y 비율을 함께 저장 |
| 자동 성공 종료 | 재사용 | 과업별 대상 상품 ID와 완료 동작으로 판정 |
| 로컬 저장 | 재사용 | 과업별 임시 저장 후 한 세션 기록으로 병합 |
| CSV | 재사용 | 과업별 다운로드가 아니라 세션 종료 시 한 번 저장 |

## 수정해야 할 부분

### 1. PC 진행자 페이지

기존 `facilitator.html`은 넓은 화면에서 과업 전체와 배정안을 한 번에 보는 구조다. 모바일에서는 긴 설명이 참여자 화면을 가리고, 진행자가 같은 기기에서 기록을 확인하기 어렵다.

모바일에서는 다음 세 화면으로 나눈다.

- `세션 홈`: 참여자, 과업 순서, 기존·개선 진행 상태
- `과업 실행`: 참여자에게 필요한 과업 문구·종료 시점·시작 버튼만 표시
- `과업 결과`: 성공 판정, SEQ, 후속 질문, 다음 조건 이동

### 2. 우측 기록 패널

기존 우측 `session-recorder`는 520px 이하에서 숨겨진다. 모바일에서는 숨겨진 상태로 자동 기록만 유지하고, 과업 중에는 참여자에게 기록 정보를 보여주지 않는다.

진행자는 세션 홈의 `기록 보기`에서 다음 값만 확인한다.

- 시작·종료 시각
- 소요 시간
- 성공·우회 성공·부분 성공·실패
- 탭 수
- 스크롤 제스처 수와 최대 깊이
- 화면 이동 경로
- SEQ와 연관성 점수

### 3. 스크롤 기록

기존 코드는 클릭 순간의 `scrollTop`만 저장하므로 스크롤 자체를 측정하지 못한다. v2에서는 다음을 별도로 기록해야 한다.

- 스크롤 제스처 수
- 스크롤 시작·종료 시각
- 방향
- 과업 중 최대 스크롤 깊이
- 대상 상품 도달 시 스크롤 위치

연속된 브라우저 스크롤 이벤트는 1회의 제스처로 묶어야 한다. 이벤트 개수를 그대로 스크롤 횟수로 사용하지 않는다.

### 4. 공통 SEQ

기존 테스트의 부담 사전·사후 문항을 공통 SEQ로 교체한다.

> 전반적으로 이 과업은 얼마나 어려웠거나 쉬웠나요?

- 1점: 매우 어려움
- 7점: 매우 쉬움

기존·개선 조건 직후 같은 문항을 제시한다. T1·T2의 연관성, T3·T4의 결과 이해 질문은 SEQ 다음에 별도로 받는다.

### 5. 참여자 배정

기존의 검색·정리·명칭 A/B 배정은 제거한다. v2에서는 P1~P5 모두 다음 순서로 진행한다.

1. 기존 서비스 T1~T3
2. 개선안 T1~T4

## 실제 지그재그 앱 기록의 한계

웹 프로토타입은 개선안 화면 안의 탭 좌표, 대상 문구, 화면 ID, 스크롤 깊이를 자동으로 기록할 수 있다. 실제 지그재그 앱 내부 행동은 브라우저 JavaScript가 접근할 수 없으므로 같은 방식으로 자동 수집할 수 없다.

### 실제 앱에서 자동 기록 가능한 것

- 모바일 세션 홈에서 `기존 서비스 시작`을 누른 시각
- 앱 수행 후 브라우저로 돌아와 `완료`를 누른 시각
- 총 소요 시간
- 참여자가 선택한 SEQ
- 진행자가 입력한 성공 판정

### 실제 앱에서 자동 기록할 수 없는 것

- 실제 앱 내부 탭 좌표
- 탭한 요소의 문구·상품 ID
- 정확한 클릭 순서
- 앱 내부 스크롤 횟수와 깊이

이 값이 필요하면 진행자가 수동으로 탭·스크롤 수를 기록하거나 화면 녹화를 사후 코딩해야 한다. 실제 앱의 수동 기록과 개선안의 자동 기록은 수집 방식이 다르므로 같은 정밀도의 수치처럼 해석하면 안 된다.

## 권장 비교 방식

### 주 비교 지표

실제 앱과 개선안에서 동일하게 확보할 수 있는 값만 직접 비교한다.

- 성공 여부
- 소요 시간
- SEQ
- 도움 요청 여부
- 대표 경로와 오해

### 보조 행동 지표

- 기존 서비스: 진행자 수동 탭·스크롤 카운트 또는 화면 녹화 코딩
- 개선안: 자동 탭·스크롤·좌표 로그

보고서에는 `기존 서비스는 관찰 기록, 개선안은 프로토타입 이벤트 로그`라고 수집 방법을 함께 표기한다.

정확한 클릭 수까지 동일 조건으로 비교하려면 실제 앱 대신 기존 화면을 재현한 모바일 웹 기준안을 사용해야 한다. 이 경우 통제력은 높아지지만 실제 서비스 자체를 사용했다는 의미는 약해진다.

## 모바일 화면 구조

### 세션 홈

- 참여자 P1~P5 선택
- 사전 질문 체크
- T1~T4 카드
- 기존·개선 진행 여부
- 누적 기록과 최종 CSV 저장

### 과업 시작

- 과업명
- 과업 문구
- 종료 시점
- `과업 시작`

기능명, 정답 경로, KPI, 성공 기준은 참여자에게 노출하지 않는다.

### 개선안 실행

- Hi-fi 화면을 기기 전체 너비로 표시
- 상단 과업 카드는 제거
- 시작 후 작은 `과업 보기` 버튼만 제공
- 과업 완료 시 자동 종료

### 기존 서비스 실행

- 시작 시각 저장
- `지그재그 앱에서 과업을 수행해 주세요` 안내
- 참여자가 앱을 직접 열어 수행
- 브라우저로 복귀 후 `완료` 또는 `중단` 선택
- 진행자가 성공 판정과 수동 탭·스크롤 수 입력

### 과업 결과

- SEQ 1~7
- 성공 판정
- 과업별 후속 질문
- 다음 조건 또는 다음 과업

## 모바일 로그에 추가할 필드

| 필드 | 설명 |
| --- | --- |
| `condition` | `before-live` 또는 `after-prototype` |
| `deviceWidth`, `deviceHeight` | 실제 CSS 뷰포트 크기 |
| `devicePixelRatio` | 기기 픽셀 비율 |
| `orientation` | 세로·가로 상태 |
| `visibilityChanges` | 앱 전환·백그라운드 횟수 참고 |
| `tapCount` | 개선안 자동, 기존안 수동 |
| `scrollGestureCount` | 개선안 자동, 기존안 수동 |
| `maxScrollDepth` | 개선안 자동 기록 |
| `completionType` | 자동 성공, 수동 완료, 중단, 시간 초과 |
| `seq` | 1~7 |
| `relevance` | T1·T2 연관성 1~7 |
| `observerNote` | 망설임·발화·오해 수동 메모 |
| `measurementMethod` | `automatic` 또는 `observer` |

## 실행 환경 주의

- iPhone에서 `file://`로 직접 열지 않고 HTTP 주소로 접속한다.
- Mac에서 서버를 열 경우 iPhone에서는 `127.0.0.1`이 아니라 Mac의 같은 와이파이 IP로 접속해야 한다.
- Safari의 동적 주소창에 대응해 `100dvh`와 safe area를 사용한다.
- 과업 중 페이지가 백그라운드로 가더라도 시작 시각은 절대 시각으로 저장해 복귀 후 소요 시간을 계산한다.
- 과업별 CSV 다운로드는 모바일에서 번거로우므로 세션 종료 후 한 파일로 저장한다.

## 구현 판단

기존 테스트에서 재사용할 핵심은 `참여자 연결`, `직접 시작`, `시간·탭 좌표 기록`, `자동 종료`, `로컬 저장`, `CSV`다. 화면 구조는 PC형 진행자 페이지를 유지하지 않고 모바일 세션 홈과 전체 화면 과업 실행 구조로 다시 만든다.

주 비교는 실제 지그재그와 개선안 모두에서 동일하게 측정 가능한 성공·시간·SEQ로 두고, 개선안의 클릭 지점과 스크롤 깊이는 원인 분석용 보조 데이터로 사용한다.
